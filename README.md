# salon-appointment-cli
Bash command line app that manages a salon appointment system using PostgreSQL. Users select a service, enter their phone number, register if new, and book an appointment time. Built with Bash scripting and SQL for simple database interaction.
